#!/usr/bin/env python3
"""npm bin entry point — delegates to the installed Python package."""

import subprocess
import sys

sys.exit(subprocess.call([sys.executable, "-m", "claude_confluence"] + sys.argv[1:]))
